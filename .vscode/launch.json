{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "TMP3 Debugger",
            "type": "debugpy",
            "request": "launch",
            "program": "src/tmp3/TMP.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Prepare for TMP3",
            "envFile": "${workspaceFolder}/devel/setup.bash"
        }
    ]
}