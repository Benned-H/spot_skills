# Specify object-relative end-effector viewpoints used to capture images
drawer:
  facing_front:
    camera_id: hand # TODO: Is this the hand camera's ID?
    pose: [1.8, 0, 1.2, 0, 0.5236]

door:
  handle:
    camera_id: hand
    pose:
      xyz_rpy: [0, 0, 0, 0, 0, 0] # TODO: Find values
      frame: "map" # TODO: Non-map frame?

board:
  facing_front:
    camera_id: hand
    pose:
      xyz_rpy: [0, 0, 0, 0, 0, 0] # TODO: Find values
      frame: "map"
